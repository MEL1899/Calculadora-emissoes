# Configuração para Render.com (opcional)
services:
  - type: web
    name: carbon-log
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GOOGLE_MAPS_API_KEY
        sync: false  # Você precisa adicionar manualmente no painel

